{
    "homepage": "https://github.com/kjkTommy/Chat",
    "name": "chatroom",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist",
        "dev": "vite",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "@types/js-cookie": "^3.0.6",
        "date-fns": "^3.6.0",
        "gh-pages": "^6.1.1",
        "js-cookie": "^3.0.5",
        "typescript": "^5.2.2",
        "vite": "^5.2.0"
    }
}
